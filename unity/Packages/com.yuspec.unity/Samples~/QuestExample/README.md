# Quest Example

This sample shows v1 quest orchestration syntax using supported event rules,
conditions, actions, and scenarios.

It keeps quest flow in text while quest system details remain in C# actions such as `start_quest`, `complete_quest`, and `give`.
