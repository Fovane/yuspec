# Quest Example

This sample shows v1.0.1 public preview quest orchestration syntax using
supported event rules, conditions, actions, and scenarios.

It keeps quest flow in text while quest system details remain in C# actions such as `start_quest`, `complete_quest`, and `give`.
