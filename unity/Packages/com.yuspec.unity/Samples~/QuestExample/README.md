# Quest Example

This sample shows zone-triggered quest orchestration.

It keeps quest flow in text while quest system details remain in C# actions such
as `start_quest`, `complete_quest`, and `give`.
