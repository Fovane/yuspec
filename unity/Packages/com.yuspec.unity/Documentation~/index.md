# YUSPEC Unity

YUSPEC is a text-based gameplay rule layer for Unity.

The package contains the Unity-facing runtime, editor debugger, action binding
support, state machine subset, scenario runner, hot reload subset, and samples.

Start with the Door example in `Samples~/DoorExample`.
